#include "main.h"

/**
 * _islower - return 1 for lowercase and 0 character
 *@c: take in a character 
 * Return: 0 for uppercase, 1 for lowercase
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z');
	{
		return (1);
	}
	else
	{
    
		return (0);
	}
}
